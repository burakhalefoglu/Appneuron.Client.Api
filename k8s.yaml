
apiVersion: v1
kind: Service
metadata:
  name: client-server-service
  namespace: service-app-dev
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    app: client-server
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-server 
  namespace: service-app-dev
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: client-server 
  template:
    metadata:
      labels:
        app: client-server 
    spec:
      nodeSelector:
        dedicated: spot
      volumes:
        - name: appconfig
          secret:
            secretName: client-service-secret
            items:
            - key: appsettings.json
              path: appsettings.json
      containers:
        - name: client-server 
          image: CONTAINER_IMAGE
          imagePullPolicy: Always
          resources:
            requests:
              memory: "150M"
              cpu: "0.05"
            limits:
              memory: "200M"
              cpu: "0.1"
          volumeMounts:
          - name: appconfig
            mountPath: /app/appsettings.json
            subPath: appsettings.json
          ports:
            - name: http-port
              containerPort: 8000
              hostPort: 8000
          livenessProbe:
            httpGet:
              path: /api/HealthChecks/get
              port: 8000
              httpHeaders:
              - name: Custom-Header
                value: Awesome
            initialDelaySeconds: 60
            periodSeconds: 5
          securityContext:
            privileged: false
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: webapi-client-ingress
  namespace: service-app-dev
spec:
  tls:
    - hosts:
        - webapi.appneuron.net
      secretName: webapi-tls
  rules:
    - host: webapi.appneuron.net
      http:
        paths:
          - path: /client
            pathType: Prefix
            backend: 
              serviceName: client-server-service
              servicePort: 8000



